\newcommand{\PreEffLength}{1em} % both pre & effs (same length)
\newcommand{\ActionWidth}{2.5em}  % width of actions
\newcommand{\ActionHeight}{2em} % height of actions

% Make sure that scheme definitions don't have empty lines; this will not compile!

\scheme{A1}{0}{
  text = {\textbf{A1}},
  pres = {},
  effs = {
    {p},
    {{$\neg$l\textsubscript{p}}}
  },
  limb length = \PreEffLength,
  height      = \ActionHeight,
  width       = \ActionWidth
}

\scheme{A2}{0}{
  text = {\textbf{A2}},
  pres = {
    {p}
  },
  effs = {
    {q},
    {\raisebox{1mm}{l\textsubscript{p}}},
    {$\neg$l\textsubscript{q}}
  },
  limb length = \PreEffLength,
  height      = 3em,
  width       = \ActionWidth
}

\scheme{A3}{0}{
  text = {\textbf{A3}},
  pres = {
    {\raisebox{2mm}{l\textsubscript{p}}}
  },
  effs = {
    {r},
    {$\neg$l\textsubscript{r}}
  },
  limb length = \PreEffLength,
  height      = \ActionHeight,
  width       = \ActionWidth
}

\scheme{init}{0}{
  %text = {\hspace*{-2mm}}, % otherwise, the empty string is at least approx 4 mm wide
  pres = {},
  effs = {
    {\raisebox{2mm}{l\textsubscript{p}}},
    {\raisebox{2mm}{l\textsubscript{q}}},
    {\raisebox{2mm}{l\textsubscript{r}}}
  },
  height      = 4em,
  width       = 0.25mm,
  limb length = 1em
}

\scheme{goal}{0}{
  %text = {\hspace*{-2mm}}, % otherwise, the empty string is at least approx 4 mm wide
  pres = { q , r },
  effs = {},
  height      = 4em,
  width       = 0.25mm,
  limb length = 1em
}