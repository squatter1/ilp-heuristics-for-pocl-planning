
%   Note that when specifying math code ($...$) in the preconditions and effects, you need to
%   put it in an *additional* set of parentheses (in addition to the ones already there). Example:
%   pres = { {A} , {B} , {C} }, BUT:
%   pres = { {A{$_1$}} , {B{$_2$}} , {C{$_1$}} }, or simply
%   pres = { {{A$_1$}} , {{B$_2$}} , {{C$_1$}} }.
%   Otherwise, you will get strange compilation errors...


% some plan for the Blocksworld domain with two grippers.
\begin{tikzpicture}
    \action{init}{init, body={draw=black, fill=black}}
    \action{A1}{A1, body = {rounded corners, draw=black, fill=white, right = 2 em of init}}
    \action{A2}{A2, body = {rounded corners, draw=black, fill=white, right = 4 em of A1}}
    \action{goal}{goal, body = {draw=black, fill=black,right = 4 em of A2}}

    \action{A3}{A3, body = {rounded corners, draw=black, fill=white, right = 6 em of A1, yshift=-3em, label={[anchor=west, xshift=-105pt, text=black]left:Additional Action/s:}}}
  
    \link{A1/eff/1}{A2/pre/1}{edge[bend left,->]}
    \link{A2/eff/1}{goal/pre/1}{edge[bend left,->]}
  \end{tikzpicture}